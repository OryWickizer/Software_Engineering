// Place for non-component helpers/constants used by context providers
// Move pure functions, constants, and utilities here to avoid exporting
// non-components from files that also export React components (fast-refresh rule).

export const noop = () => {};
